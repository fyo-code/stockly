# Stockly Site Demo

Standalone, sanitized interactive site demo for Stockly.

This folder intentionally contains only the public-facing demo site. It does not include the original project backend, source CSVs, business docs, memory/context files, or operational dashboard data.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.
